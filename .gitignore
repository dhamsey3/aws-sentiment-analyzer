# Ignore Terraform state and variable files
terraform/**/*.tfstate
terraform/**/*.tfstate.backup
terraform/**/*.tfvars
terraform/.terraform/

# Backend config local override (do not commit real backend configs)
terraform/backend.tf.local

# Lambda artifacts
terraform/lambda/*.zip
terraform/lambda/build/

# Python artifacts and build metadata
**/__pycache__/
**/*.pyc
**/*.pyo
**/*.dist-info/
**/*.egg-info/

# Virtualenvs and env files
.venv/
venv/
.env
.env.*
